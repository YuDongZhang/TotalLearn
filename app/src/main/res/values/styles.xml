<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:listDivider">@drawable/md_divider</item>
<!-- 状态栏颜色            <item name="android:statusBarColor">#ff0000</item>-->
<!-- 状态栏颜色,透明色      <item name="android:statusBarColor">@android:color/transparent</item>-->
<!-- 状态栏透明            <item name="android:windowTranslucentStatus">true</item>-->
<!-- 虚拟导航栏透明        <item name="android:windowTranslucentNavigation">true</item>-->

    </style>

    <style name="dialog" parent="android:Widget.ProgressBar.Small">
        <item name="android:windowIsFloating">true</item>
        <!-- 设置未浮动窗口 -->
        <item name="android:windowFrame">@null</item>
        <!-- 设置无边框 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 设置无标题 -->
        <item name="android:windowBackground">@color/transparent</item>
        <!-- 设置完全透明 -->
        <item name="android:backgroundDimEnabled">true</item>
        <!--  背景的明暗度    -->
        <item name="android:backgroundDimAmount">0.0</item>
        <!-- 是否模糊 -->
        <item name="android:windowIsTranslucent">true</item>
        <!-- 半透明 -->
    </style>

</resources>
